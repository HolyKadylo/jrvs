#!/usr/bin/env python3
# TODO: pythonOutput -- a Tkinter window that displays program responses for the
# python channel. It should subscribe to the "output.events" bus channel (see
# bus.tail) and append each response to a read-only, scrolling text area, prefixed
# with "<project folder name>> " (picked at startup), mirroring how bashOutput.sh
# renders the bash channel. Not implemented yet.
